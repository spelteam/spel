language: cpp
script: cmake
compiler:
  - gcc
#  - clang

env:
  matrix:
    - SPEL_BUILD_TYPE=linux

matrix:
  fast_finish: true

script:
  - if [[ "${SPEL_BUILD_TYPE}" == "linux" ]]; then ( mkdir build && cd build && ../linux_install_dependencies.sh cmake ../src/ && make ) fi
